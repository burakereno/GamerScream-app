# Production secrets are supplied through LIVEKIT_KEYS, never committed here.
port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: true

logging:
  level: info

room:
  auto_create: true
  empty_timeout: 300
  max_participants: 10
